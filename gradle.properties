org.gradle.parallel=true
org.gradle.caching=true

# Single source of truth for the published version (library + natives artifacts).
# Release tags must match: tag v0.0.1 <-> version=0.0.1.
group=io.github.refux
version=0.0.3
