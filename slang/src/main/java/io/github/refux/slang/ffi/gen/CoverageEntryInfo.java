// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Per-coverage-entry attribution returned by
 * `ICoverageTracingMetadata::getEntryInfo`. Use the leading
 * `structSize` for ABI-versioned struct growth: future revisions
 * may add fields such as column/span information, function identity,
 * branch-arm identity, or coverage-mode-specific metadata at the end
 * without changing the COM interface. Entries are source-location
 * based: the current producers emit one source entry per marker op.
 * If multiple line entries resolve to the same `(file, line)`, LCOV
 * export aggregates them at report-generation time.
 *
 * Layout of C struct {&#64;code CoverageEntryInfo} (size 80, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class CoverageEntryInfo {
    private CoverageEntryInfo() {}

    public static final long SIZE = 80;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_structSize = 0;
    public static final long OFFSET_file = 8;
    public static final long OFFSET_line = 16;
    public static final long OFFSET_counterIndex = 20;
    public static final long OFFSET_kind = 24;
    public static final long OFFSET_counterMode = 28;
    public static final long OFFSET_startColumn = 32;
    public static final long OFFSET_endLine = 36;
    public static final long OFFSET_endColumn = 40;
    public static final long OFFSET_functionName = 48;
    public static final long OFFSET_functionMangledName = 56;
    public static final long OFFSET_branchSiteID = 64;
    public static final long OFFSET_branchArmID = 68;
    public static final long OFFSET_branchArmKind = 72;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static long getStructSize(MemorySegment self) {
        return self.get(FfiSupport.C_SIZE_T, OFFSET_structSize);
    }

    public static void setStructSize(MemorySegment self, long value) {
        self.set(FfiSupport.C_SIZE_T, OFFSET_structSize, value);
    }

    /**
     * Source file for this coverage entry, or `nullptr` if the entry
     * could not be attributed to a real source file. The returned
     * pointer is valid for the lifetime of the metadata object.
     */
    public static MemorySegment getFile(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_file);
    }

    public static void setFile(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_file, value);
    }

    /**
     * 1-based source line for this coverage entry, or 0 if the entry
     * could not be attributed to a real source line. The current
     * implementation reports line, function, and branch entries;
     * future revisions may add source-region entries or additional
     * branch forms.
     */
    public static int getLine(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_line);
    }

    public static void setLine(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_line, value);
    }

    /**
     * Counter slot used by this entry, or
     * `kInvalidCoverageCounterIndex` when the entry has no runtime
     * counter. The current line/function/branch producers use one
     * direct counter per entry. Future source-region coverage may use
     * `kInvalidCoverageCounterIndex` for entries whose count is
     * derived from other counters or represented through tail-extended
     * fields.
     */
    public static int getCounterIndex(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_counterIndex);
    }

    public static void setCounterIndex(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_counterIndex, value);
    }

    /**
     * Semantic kind of this source coverage entry.
     */
    public static int getKind(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_kind);
    }

    public static void setKind(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_kind, value);
    }

    /**
     * Runtime accumulation mode for `counterIndex`. The current
     * implementation only defines `Count`; future concrete modes can
     * be appended when implemented.
     */
    public static int getCounterMode(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_counterMode);
    }

    public static void setCounterMode(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_counterMode, value);
    }

    /**
     * 1-based inclusive start column for this entry, or 0 when
     * unavailable.
     */
    public static int getStartColumn(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_startColumn);
    }

    public static void setStartColumn(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_startColumn, value);
    }

    /**
     * 1-based end line for this entry's half-open end coordinate, or
     * 0 when the exact range is unavailable. Future source-region
     * coverage can use `(line,startColumn)` to `(endLine,endColumn)`
     * for a half-open source range.
     */
    public static int getEndLine(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_endLine);
    }

    public static void setEndLine(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_endLine, value);
    }

    /**
     * 1-based exclusive end column for this entry, or 0 when
     * unavailable.
     */
    public static int getEndColumn(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_endColumn);
    }

    public static void setEndColumn(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_endColumn, value);
    }

    /**
     * Function display name for function coverage entries, or
     * `nullptr` when not applicable or unavailable.
     */
    public static MemorySegment getFunctionName(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_functionName);
    }

    public static void setFunctionName(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_functionName, value);
    }

    /**
     * Stable mangled function name for function coverage entries, or
     * `nullptr` when not applicable or unavailable.
     */
    public static MemorySegment getFunctionMangledName(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_functionMangledName);
    }

    public static void setFunctionMangledName(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_functionMangledName, value);
    }

    /**
     * Stable branch-site identifier within this metadata object, or 0
     * when not applicable.
     */
    public static int getBranchSiteID(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_branchSiteID);
    }

    public static void setBranchSiteID(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_branchSiteID, value);
    }

    /**
     * Stable branch-arm identifier within `branchSiteID`, or 0 when
     * not applicable.
     */
    public static int getBranchArmID(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_branchArmID);
    }

    public static void setBranchArmID(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_branchArmID, value);
    }

    /**
     * Branch arm semantic for branch coverage entries.
     */
    public static int getBranchArmKind(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_branchArmKind);
    }

    public static void setBranchArmKind(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_branchArmKind, value);
    }

}
