// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodType;
import static java.lang.foreign.ValueLayout.*;

/**
 * A session provides a scope for code that is loaded.
 * 
 * A session can be used to load modules of Slang source code,
 * and to request target-specific compiled binaries and layout
 * information.
 * 
 * In order to be able to load code, the session owns a set
 * of active "search paths" for resolving `#include` directives
 * and `import` declarations, as well as a set of global
 * preprocessor definitions that will be used for all code
 * that gets `import`ed in the session.
 * 
 * If multiple user shaders are loaded in the same session,
 * and import the same module (e.g., two source files do `import X`)
 * then there will only be one copy of `X` loaded within the session.
 * 
 * In order to be able to generate target code, the session
 * owns a list of available compilation targets, which specify
 * code generation options.
 * 
 * Code loaded and compiled within a session is owned by the session
 * and will remain resident in memory until the session is released.
 * Applications wishing to control the memory usage for compiled
 * and loaded code should use multiple sessions.
 *
 * Raw vtable dispatch for COM interface {&#64;code ISession} (base {&#64;code ISlangUnknown}, vtable size 24). Methods are static and take the COM object pointer as {&#64;code self}; inherited slots live on the base interface's class. The hand-written wrappers in {&#64;code ffi} add ownership and ergonomics.
 */
public final class ISession {
    private ISession() {}

    /** SLANG_COM_INTERFACE uuid, for queryInterface. */
    public static final String IID = "67618701-d116-468f-ab3b-474bedce0e3d";

    public static final int VT_getGlobalSession = 3;
    public static final int VT_loadModule = 4;
    public static final int VT_loadModuleFromSource = 5;
    public static final int VT_createCompositeComponentType = 6;
    public static final int VT_specializeType = 7;
    public static final int VT_getTypeLayout = 8;
    public static final int VT_getContainerType = 9;
    public static final int VT_getDynamicType = 10;
    public static final int VT_getTypeRTTIMangledName = 11;
    public static final int VT_getTypeConformanceWitnessMangledName = 12;
    public static final int VT_getTypeConformanceWitnessSequentialID = 13;
    public static final int VT_createCompileRequest = 14;
    public static final int VT_createTypeConformanceComponentType = 15;
    public static final int VT_loadModuleFromIRBlob = 16;
    public static final int VT_getLoadedModuleCount = 17;
    public static final int VT_getLoadedModule = 18;
    public static final int VT_isBinaryModuleUpToDate = 19;
    public static final int VT_loadModuleFromSourceString = 20;
    public static final int VT_getDynamicObjectRTTIBytes = 21;
    public static final int VT_loadModuleInfoFromIRBlob = 22;
    public static final int VT_getDeclSourceLocation = 23;

    private static final MethodHandle MH_A_A =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS));
    private static final MethodHandle MH_A_AAA =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_A_AAAAA =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_A_AAAJA =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_A_AAIA =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS, ADDRESS, JAVA_INT, ADDRESS));
    private static final MethodHandle MH_A_AAJIA =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, JAVA_INT, ADDRESS));
    private static final MethodHandle MH_A_AJ =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS, JAVA_LONG));
    private static final MethodHandle MH_I_AA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAAAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAAAI =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, JAVA_INT));
    private static final MethodHandle MH_I_AAAAJA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_I_AAJAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS, ADDRESS));
    private static final MethodHandle MH_J_A =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_Z_AAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_BOOLEAN, ADDRESS, ADDRESS, ADDRESS));

    /**
     * Get the global session thas was used to create this session.
     *
     * Vtable slot {&#64;value #VT_getGlobalSession}.
     */
    public static MemorySegment getGlobalSession(MemorySegment self) {
        try {
            return (MemorySegment) MH_A_A.invokeExact(FfiSupport.fnPtr(self, VT_getGlobalSession), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Load a module as it would be by code using `import`.
     *
     * Vtable slot {&#64;value #VT_loadModule}.
     */
    public static MemorySegment loadModule(MemorySegment self, MemorySegment moduleName, MemorySegment outDiagnostics) {
        try {
            return (MemorySegment) MH_A_AAA.invokeExact(FfiSupport.fnPtr(self, VT_loadModule), self, moduleName, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Load a module from Slang source code.
     *
     * Vtable slot {&#64;value #VT_loadModuleFromSource}.
     */
    public static MemorySegment loadModuleFromSource(MemorySegment self, MemorySegment moduleName, MemorySegment path, MemorySegment source, MemorySegment outDiagnostics) {
        try {
            return (MemorySegment) MH_A_AAAAA.invokeExact(FfiSupport.fnPtr(self, VT_loadModuleFromSource), self, moduleName, path, source, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Combine multiple component types to create a composite component type.
     * 
     * The `componentTypes` array must contain `componentTypeCount` pointers
     * to component types that were loaded or created using the same session.
     * 
     * The shader parameters and specialization parameters of the composite will
     * be the union of those in `componentTypes`. The relative order of child
     * component types is significant, and will affect the order in which
     * parameters are reflected and laid out.
     * 
     * The entry-point functions of the composite will be the union of those in
     * `componentTypes`, and will follow the ordering of `componentTypes`.
     * 
     * The requirements of the composite component type will be a subset of
     * those in `componentTypes`. If an entry in `componentTypes` has a requirement
     * that can be satisfied by another entry, then the composition will
     * satisfy the requirement and it will not appear as a requirement of
     * the composite. If multiple entries in `componentTypes` have a requirement
     * for the same type, then only the first such requirement will be retained
     * on the composite. The relative ordering of requirements on the composite
     * will otherwise match that of `componentTypes`.
     * 
     * If any diagnostics are generated during creation of the composite, they
     * will be written to `outDiagnostics`. If an error is encountered, the
     * function will return null.
     * 
     * It is an error to create a composite component type that recursively
     * aggregates a single module more than once.
     *
     * Vtable slot {&#64;value #VT_createCompositeComponentType}.
     */
    public static int createCompositeComponentType(MemorySegment self, MemorySegment componentTypes, long componentTypeCount, MemorySegment outCompositeComponentType, MemorySegment outDiagnostics) {
        try {
            return (int) MH_I_AAJAA.invokeExact(FfiSupport.fnPtr(self, VT_createCompositeComponentType), self, componentTypes, componentTypeCount, outCompositeComponentType, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Specialize a type based on type arguments.
     *
     * Vtable slot {&#64;value #VT_specializeType}.
     */
    public static MemorySegment specializeType(MemorySegment self, MemorySegment type, MemorySegment specializationArgs, long specializationArgCount, MemorySegment outDiagnostics) {
        try {
            return (MemorySegment) MH_A_AAAJA.invokeExact(FfiSupport.fnPtr(self, VT_specializeType), self, type, specializationArgs, specializationArgCount, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the layout `type` on the chosen `target`.
     *
     * Vtable slot {&#64;value #VT_getTypeLayout}.
     */
    public static MemorySegment getTypeLayout(MemorySegment self, MemorySegment type, long targetIndex, int rules, MemorySegment outDiagnostics) {
        try {
            return (MemorySegment) MH_A_AAJIA.invokeExact(FfiSupport.fnPtr(self, VT_getTypeLayout), self, type, targetIndex, rules, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get a container type from `elementType`. For example, given type `T`, returns
     * a type that represents `StructuredBuffer&lt;T&gt;`.
     * 
     * &#64;param `elementType`: the element type to wrap around.
     * &#64;param `containerType`: the type of the container to wrap `elementType` in.
     * &#64;param `outDiagnostics`: a blob to receive diagnostic messages.
     *
     * Vtable slot {&#64;value #VT_getContainerType}.
     */
    public static MemorySegment getContainerType(MemorySegment self, MemorySegment elementType, int containerType, MemorySegment outDiagnostics) {
        try {
            return (MemorySegment) MH_A_AAIA.invokeExact(FfiSupport.fnPtr(self, VT_getContainerType), self, elementType, containerType, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Return a `TypeReflection` that represents the `__Dynamic` type.
     * This type can be used as a specialization argument to indicate using
     * dynamic dispatch.
     *
     * Vtable slot {&#64;value #VT_getDynamicType}.
     */
    public static MemorySegment getDynamicType(MemorySegment self) {
        try {
            return (MemorySegment) MH_A_A.invokeExact(FfiSupport.fnPtr(self, VT_getDynamicType), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the mangled name for a type RTTI object.
     *
     * Vtable slot {&#64;value #VT_getTypeRTTIMangledName}.
     */
    public static int getTypeRTTIMangledName(MemorySegment self, MemorySegment type, MemorySegment outNameBlob) {
        try {
            return (int) MH_I_AAA.invokeExact(FfiSupport.fnPtr(self, VT_getTypeRTTIMangledName), self, type, outNameBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the mangled name for a type witness.
     *
     * Vtable slot {&#64;value #VT_getTypeConformanceWitnessMangledName}.
     */
    public static int getTypeConformanceWitnessMangledName(MemorySegment self, MemorySegment type, MemorySegment interfaceType, MemorySegment outNameBlob) {
        try {
            return (int) MH_I_AAAA.invokeExact(FfiSupport.fnPtr(self, VT_getTypeConformanceWitnessMangledName), self, type, interfaceType, outNameBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the sequential ID used to identify a type witness in a dynamic object.
     * The sequential ID is part of the RTTI bytes returned by `getDynamicObjectRTTIBytes`.
     *
     * Vtable slot {&#64;value #VT_getTypeConformanceWitnessSequentialID}.
     */
    public static int getTypeConformanceWitnessSequentialID(MemorySegment self, MemorySegment type, MemorySegment interfaceType, MemorySegment outId) {
        try {
            return (int) MH_I_AAAA.invokeExact(FfiSupport.fnPtr(self, VT_getTypeConformanceWitnessSequentialID), self, type, interfaceType, outId);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Create a request to load/compile front-end code.
     *
     * Vtable slot {&#64;value #VT_createCompileRequest}.
     */
    public static int createCompileRequest(MemorySegment self, MemorySegment outCompileRequest) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_createCompileRequest), self, outCompileRequest);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Creates a `IComponentType` that represents a type's conformance to an interface.
     * The retrieved `ITypeConformance` objects can be included in a composite `IComponentType`
     * to explicitly specify which implementation types should be included in the final compiled
     * code. For example, if an module defines `IMaterial` interface and `AMaterial`,
     * `BMaterial`, `CMaterial` types that implements the interface, the user can exclude
     * `CMaterial` implementation from the resulting shader code by explicitly adding
     * `AMaterial:IMaterial` and `BMaterial:IMaterial` conformances to a composite
     * `IComponentType` and get entry point code from it. The resulting code will not have
     * anything related to `CMaterial` in the dynamic dispatch logic. If the user does not
     * explicitly include any `TypeConformances` to an interface type, all implementations to
     * that interface will be included by default. By linking a `ITypeConformance`, the user is
     * also given the opportunity to specify the dispatch ID of the implementation type. If
     * `conformanceIdOverride` is -1, there will be no override behavior and Slang will
     * automatically assign IDs to implementation types. The automatically assigned IDs can be
     * queried via `ISession::getTypeConformanceWitnessSequentialID`.
     * 
     * Returns SLANG_OK if succeeds, or SLANG_FAIL if `type` does not conform to `interfaceType`.
     *
     * Vtable slot {&#64;value #VT_createTypeConformanceComponentType}.
     */
    public static int createTypeConformanceComponentType(MemorySegment self, MemorySegment type, MemorySegment interfaceType, MemorySegment outConformance, long conformanceIdOverride, MemorySegment outDiagnostics) {
        try {
            return (int) MH_I_AAAAJA.invokeExact(FfiSupport.fnPtr(self, VT_createTypeConformanceComponentType), self, type, interfaceType, outConformance, conformanceIdOverride, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Load a module from a Slang module blob.
     *
     * Vtable slot {&#64;value #VT_loadModuleFromIRBlob}.
     */
    public static MemorySegment loadModuleFromIRBlob(MemorySegment self, MemorySegment moduleName, MemorySegment path, MemorySegment source, MemorySegment outDiagnostics) {
        try {
            return (MemorySegment) MH_A_AAAAA.invokeExact(FfiSupport.fnPtr(self, VT_loadModuleFromIRBlob), self, moduleName, path, source, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Vtable slot {&#64;value #VT_getLoadedModuleCount}.
     */
    public static long getLoadedModuleCount(MemorySegment self) {
        try {
            return (long) MH_J_A.invokeExact(FfiSupport.fnPtr(self, VT_getLoadedModuleCount), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Vtable slot {&#64;value #VT_getLoadedModule}.
     */
    public static MemorySegment getLoadedModule(MemorySegment self, long index) {
        try {
            return (MemorySegment) MH_A_AJ.invokeExact(FfiSupport.fnPtr(self, VT_getLoadedModule), self, index);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Checks if a precompiled binary module is up-to-date with the current compiler
     * option settings and the source file contents.
     * 
     * When the module's primary source file cannot be located on the search paths, the
     * binary module is treated as a standalone artifact and reported as up-to-date so
     * that callers distributing precompiled-only modules can load them. In that case the
     * compiler-version and option-set hash carried in the binary are NOT compared. If a
     * later (secondary) dependency is missing the module is still reported as stale.
     *
     * Vtable slot {&#64;value #VT_isBinaryModuleUpToDate}.
     */
    public static boolean isBinaryModuleUpToDate(MemorySegment self, MemorySegment modulePath, MemorySegment binaryModuleBlob) {
        try {
            return (boolean) MH_Z_AAA.invokeExact(FfiSupport.fnPtr(self, VT_isBinaryModuleUpToDate), self, modulePath, binaryModuleBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Load a module from a string.
     *
     * Vtable slot {&#64;value #VT_loadModuleFromSourceString}.
     */
    public static MemorySegment loadModuleFromSourceString(MemorySegment self, MemorySegment moduleName, MemorySegment path, MemorySegment string, MemorySegment outDiagnostics) {
        try {
            return (MemorySegment) MH_A_AAAAA.invokeExact(FfiSupport.fnPtr(self, VT_loadModuleFromSourceString), self, moduleName, path, string, outDiagnostics);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the 16-byte RTTI header to fill into a dynamic object.
     * This header is used to identify the type of the object for dynamic dispatch purpose.
     * For example, given the following shader:
     * 
     * ```slang
     * [anyValueSize(32)] dyn interface IFoo { int eval(); }
     * struct Impl : IFoo { int eval() { return 1; } }
     * 
     * ConstantBuffer&lt;dyn IFoo&gt; cb0;
     * 
     * [numthreads(1,1,1)
     * void main()
     * {
     * cb0.eval();
     * }
     * ```
     * 
     * The constant buffer `cb0` should be filled with 16+32=48 bytes of data, where the first
     * 16 bytes should be the RTTI bytes returned by calling `getDynamicObjectRTTIBytes(type_Impl,
     * type_IFoo)`, and the rest 32 bytes should hold the actual data of the dynamic object (in
     * this case, fields in the `Impl` type).
     * 
     * `bufferSizeInBytes` must be greater than 16.
     *
     * Vtable slot {&#64;value #VT_getDynamicObjectRTTIBytes}.
     */
    public static int getDynamicObjectRTTIBytes(MemorySegment self, MemorySegment type, MemorySegment interfaceType, MemorySegment outRTTIDataBuffer, int bufferSizeInBytes) {
        try {
            return (int) MH_I_AAAAI.invokeExact(FfiSupport.fnPtr(self, VT_getDynamicObjectRTTIBytes), self, type, interfaceType, outRTTIDataBuffer, bufferSizeInBytes);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Read module info (name and version) from a module blob
     * 
     * The returned pointers are valid for as long as the session.
     *
     * Vtable slot {&#64;value #VT_loadModuleInfoFromIRBlob}.
     */
    public static int loadModuleInfoFromIRBlob(MemorySegment self, MemorySegment source, MemorySegment outModuleVersion, MemorySegment outModuleCompilerVersion, MemorySegment outModuleName) {
        try {
            return (int) MH_I_AAAAA.invokeExact(FfiSupport.fnPtr(self, VT_loadModuleInfoFromIRBlob), self, source, outModuleVersion, outModuleCompilerVersion, outModuleName);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the source location of a declaration.
     * 
     * The returned filePath pointer is valid for as long as the session.
     *
     * Vtable slot {&#64;value #VT_getDeclSourceLocation}.
     */
    public static int getDeclSourceLocation(MemorySegment self, MemorySegment decl, MemorySegment outLocation) {
        try {
            return (int) MH_I_AAA.invokeExact(FfiSupport.fnPtr(self, VT_getDeclSourceLocation), self, decl, outLocation);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

}
