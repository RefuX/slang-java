// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodType;
import static java.lang.foreign.ValueLayout.*;

/**
 * Raw vtable dispatch for COM interface {&#64;code ISlangMutableFileSystem} (base {&#64;code ISlangFileSystemExt}, vtable size 16). Methods are static and take the COM object pointer as {&#64;code self}; inherited slots live on the base interface's class. The hand-written wrappers in {&#64;code ffi} add ownership and ergonomics.
 */
public final class ISlangMutableFileSystem {
    private ISlangMutableFileSystem() {}

    /** SLANG_COM_INTERFACE uuid, for queryInterface. */
    public static final String IID = "a058675c-1d65-452a-8458-ccded1427105";

    public static final int VT_saveFile = 12;
    public static final int VT_saveFileBlob = 13;
    public static final int VT_remove = 14;
    public static final int VT_createDirectory = 15;

    private static final MethodHandle MH_I_AA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAAz =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, FfiSupport.C_SIZE_T));

    /**
     * Write data to the specified path.
     * 
     * &#64;param path The path for data to be saved to
     * &#64;param data The data to be saved
     * &#64;param size The size of the data in bytes
     * &#64;returns SLANG_OK if successful (SLANG_E_NOT_IMPLEMENTED if not implemented, or some other
     * error code)
     *
     * Vtable slot {&#64;value #VT_saveFile}.
     */
    public static int saveFile(MemorySegment self, MemorySegment path, MemorySegment data, long size) {
        try {
            return (int) MH_I_AAAz.invokeExact(FfiSupport.fnPtr(self, VT_saveFile), self, path, data, size);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Write data in the form of a blob to the specified path.
     * 
     * Depending on the implementation writing a blob might be faster/use less memory. It is
     * assumed the blob is *immutable* and that an implementation can reference count it.
     * 
     * It is not guaranteed loading the same file will return the *same* blob - just a blob with
     * same contents.
     * 
     * &#64;param path The path for data to be saved to
     * &#64;param dataBlob The data to be saved
     * &#64;returns SLANG_OK if successful (SLANG_E_NOT_IMPLEMENTED if not implemented, or some other
     * error code)
     *
     * Vtable slot {&#64;value #VT_saveFileBlob}.
     */
    public static int saveFileBlob(MemorySegment self, MemorySegment path, MemorySegment dataBlob) {
        try {
            return (int) MH_I_AAA.invokeExact(FfiSupport.fnPtr(self, VT_saveFileBlob), self, path, dataBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Remove the entry in the path (directory of file). Will only delete an empty directory,
     * if not empty will return an error.
     * 
     * &#64;param path The path to remove
     * &#64;returns SLANG_OK if successful
     *
     * Vtable slot {&#64;value #VT_remove}.
     */
    public static int remove(MemorySegment self, MemorySegment path) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_remove), self, path);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Create a directory.
     * 
     * The path to the directory must exist
     * 
     * &#64;param path To the directory to create. The parent path *must* exist otherwise will return
     * an error.
     * &#64;returns SLANG_OK if successful
     *
     * Vtable slot {&#64;value #VT_createDirectory}.
     */
    public static int createDirectory(MemorySegment self, MemorySegment path) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_createDirectory), self, path);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

}
