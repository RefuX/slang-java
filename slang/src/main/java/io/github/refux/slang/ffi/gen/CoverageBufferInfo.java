// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Coverage-buffer descriptor binding info returned by
 * `ICoverageTracingMetadata::getBufferInfo`. This coverage-specific
 * view is kept for ABI compatibility. New host integrations should
 * query `ISyntheticResourceMetadata` when they need the full hidden-
 * resource binding contract, including CPU/CUDA marshaling fields.
 * 
 * Sentinel conventions:
 * - `space == -1` means no descriptor space is reported for the
 * current target
 * - `binding == -1` means no descriptor binding is reported for the
 * current target
 * `0` is a valid value for both fields.
 *
 * Layout of C struct {&#64;code CoverageBufferInfo} (size 24, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class CoverageBufferInfo {
    private CoverageBufferInfo() {}

    public static final long SIZE = 24;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_structSize = 0;
    public static final long OFFSET_space = 8;
    public static final long OFFSET_binding = 12;
    public static final long OFFSET_elementByteWidth = 16;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static long getStructSize(MemorySegment self) {
        return self.get(FfiSupport.C_SIZE_T, OFFSET_structSize);
    }

    public static void setStructSize(MemorySegment self, long value) {
        self.set(FfiSupport.C_SIZE_T, OFFSET_structSize, value);
    }

    /**
     * Register space the coverage buffer is bound to (D3D12
     * `space`, Vulkan descriptor set), or -1 if not assigned for
     * this target.
     */
    public static int getSpace(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_space);
    }

    public static void setSpace(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_space, value);
    }

    /**
     * Binding index the coverage buffer is bound at (D3D12
     * `register`, Vulkan `binding`), or -1 if not assigned for
     * this target.
     */
    public static int getBinding(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_binding);
    }

    public static void setBinding(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_binding, value);
    }

    /**
     * Byte width of one counter slot in the synthesized buffer:
     * `4` for a `RWStructuredBuffer&lt;uint&gt;`, `8` for a
     * `RWStructuredBuffer&lt;uint64_t&gt;`. The host reads back
     * `getCounterCount() * elementByteWidth` bytes and interprets
     * each slot as a little-endian unsigned integer of this width.
     * Mirrored on the JSON sidecar as `buffer.element_stride`.
     * 
     * A current in-process implementation always writes `4` or `8`;
     * the in-class default `4` only appears if the caller forgot to
     * pass the field to `getBufferInfo`. A sentinel `0` can only
     * arise when reading a metadata object from an older compiler
     * that pre-dates this field; both values should be treated as
     * the historical uint32 layout.
     */
    public static int getElementByteWidth(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_elementByteWidth);
    }

    public static void setElementByteWidth(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_elementByteWidth, value);
    }

}
