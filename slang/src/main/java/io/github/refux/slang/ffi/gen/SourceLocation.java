// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Layout of C struct {&#64;code SourceLocation} (size 24, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class SourceLocation {
    private SourceLocation() {}

    public static final long SIZE = 24;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_filePath = 0;
    public static final long OFFSET_line = 8;
    public static final long OFFSET_column = 16;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static MemorySegment getFilePath(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_filePath);
    }

    public static void setFilePath(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_filePath, value);
    }

    public static long getLine(MemorySegment self) {
        return self.get(JAVA_LONG, OFFSET_line);
    }

    public static void setLine(MemorySegment self, long value) {
        self.set(JAVA_LONG, OFFSET_line, value);
    }

    public static long getColumn(MemorySegment self) {
        return self.get(JAVA_LONG, OFFSET_column);
    }

    public static void setColumn(MemorySegment self, long value) {
        self.set(JAVA_LONG, OFFSET_column, value);
    }

}
