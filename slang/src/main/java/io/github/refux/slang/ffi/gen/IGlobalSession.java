// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodType;
import static java.lang.foreign.ValueLayout.*;

/**
 * A global session for interaction with the Slang library.
 * 
 * An application may create and re-use a single global session across
 * multiple sessions, in order to amortize startups costs (in current
 * Slang this is mostly the cost of loading the Slang standard library).
 * 
 * A single global session object is currently *not* thread-safe. Unless
 * documented otherwise, a global session and the objects created from it
 * should be externally synchronized when shared across threads. Distinct
 * global sessions may be used from different threads in parallel.
 *
 * Raw vtable dispatch for COM interface {&#64;code IGlobalSession} (base {&#64;code ISlangUnknown}, vtable size 33). Methods are static and take the COM object pointer as {&#64;code self}; inherited slots live on the base interface's class. The hand-written wrappers in {&#64;code ffi} add ownership and ergonomics.
 */
public final class IGlobalSession {
    private IGlobalSession() {}

    /** SLANG_COM_INTERFACE uuid, for queryInterface. */
    public static final String IID = "c140b5fd-0c78-452e-ba7c-1a1e70c7f71c";

    public static final int VT_createSession = 3;
    public static final int VT_findProfile = 4;
    public static final int VT_setDownstreamCompilerPath = 5;
    public static final int VT_setDownstreamCompilerPrelude = 6;
    public static final int VT_getDownstreamCompilerPrelude = 7;
    public static final int VT_getBuildTagString = 8;
    public static final int VT_setDefaultDownstreamCompiler = 9;
    public static final int VT_getDefaultDownstreamCompiler = 10;
    public static final int VT_setLanguagePrelude = 11;
    public static final int VT_getLanguagePrelude = 12;
    public static final int VT_createCompileRequest = 13;
    public static final int VT_addBuiltins = 14;
    public static final int VT_setSharedLibraryLoader = 15;
    public static final int VT_getSharedLibraryLoader = 16;
    public static final int VT_checkCompileTargetSupport = 17;
    public static final int VT_checkPassThroughSupport = 18;
    public static final int VT_compileCoreModule = 19;
    public static final int VT_loadCoreModule = 20;
    public static final int VT_saveCoreModule = 21;
    public static final int VT_findCapability = 22;
    public static final int VT_setDownstreamCompilerForTransition = 23;
    public static final int VT_getDownstreamCompilerForTransition = 24;
    public static final int VT_getCompilerElapsedTime = 25;
    public static final int VT_setSPIRVCoreGrammar = 26;
    public static final int VT_parseCommandLineArguments = 27;
    public static final int VT_getSessionDescDigest = 28;
    public static final int VT_compileBuiltinModule = 29;
    public static final int VT_loadBuiltinModule = 30;
    public static final int VT_saveBuiltinModule = 31;
    public static final int VT_getDownstreamCompilerVersion = 32;

    private static final MethodHandle MH_A_A =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AAz =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, FfiSupport.C_SIZE_T));
    private static final MethodHandle MH_I_AI =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT));
    private static final MethodHandle MH_I_AIA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, ADDRESS));
    private static final MethodHandle MH_I_AIAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AIAAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AIAz =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, ADDRESS, FfiSupport.C_SIZE_T));
    private static final MethodHandle MH_I_AII =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_I_AIIA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, JAVA_INT, ADDRESS));
    private static final MethodHandle MH_V_AA =
        FfiSupport.unbound(FunctionDescriptor.ofVoid(ADDRESS, ADDRESS));
    private static final MethodHandle MH_V_AAA =
        FfiSupport.unbound(FunctionDescriptor.ofVoid(ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_V_AIA =
        FfiSupport.unbound(FunctionDescriptor.ofVoid(ADDRESS, JAVA_INT, ADDRESS));
    private static final MethodHandle MH_V_AIII =
        FfiSupport.unbound(FunctionDescriptor.ofVoid(ADDRESS, JAVA_INT, JAVA_INT, JAVA_INT));

    /**
     * Create a new session for loading and compiling code.
     *
     * Vtable slot {&#64;value #VT_createSession}.
     */
    public static int createSession(MemorySegment self, MemorySegment desc, MemorySegment outSession) {
        try {
            return (int) MH_I_AAA.invokeExact(FfiSupport.fnPtr(self, VT_createSession), self, desc, outSession);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Look up the internal ID of a profile by its `name`.
     * 
     * Profile IDs are *not* guaranteed to be stable across versions
     * of the Slang library, so clients are expected to look up
     * profiles by name at runtime.
     *
     * Vtable slot {&#64;value #VT_findProfile}.
     */
    public static int findProfile(MemorySegment self, MemorySegment name) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_findProfile), self, name);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Set the path that downstream compilers (aka back end compilers) will
     * be looked from.
     * &#64;param passThrough Identifies the downstream compiler
     * &#64;param path The path to find the downstream compiler (shared library/dll/executable)
     * 
     * For back ends that are dlls/shared libraries, it will mean the path will
     * be prefixed with the path when calls are made out to ISlangSharedLibraryLoader.
     * For executables - it will look for executables along the path
     *
     * Vtable slot {&#64;value #VT_setDownstreamCompilerPath}.
     */
    public static void setDownstreamCompilerPath(MemorySegment self, int passThrough, MemorySegment path) {
        try {
            MH_V_AIA.invokeExact(FfiSupport.fnPtr(self, VT_setDownstreamCompilerPath), self, passThrough, path);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * DEPRECATED: Use setLanguagePrelude
     * 
     * Set the 'prelude' for generated code for a 'downstream compiler'.
     * &#64;param passThrough The downstream compiler for generated code that will have the prelude applied
     * to it.
     * &#64;param preludeText The text added pre-pended verbatim before the generated source
     * 
     * That for pass-through usage, prelude is not pre-pended, preludes are for code generation only.
     *
     * Vtable slot {&#64;value #VT_setDownstreamCompilerPrelude}.
     */
    public static void setDownstreamCompilerPrelude(MemorySegment self, int passThrough, MemorySegment preludeText) {
        try {
            MH_V_AIA.invokeExact(FfiSupport.fnPtr(self, VT_setDownstreamCompilerPrelude), self, passThrough, preludeText);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * DEPRECATED: Use getLanguagePrelude
     * 
     * Get the 'prelude' for generated code for a 'downstream compiler'.
     * &#64;param passThrough The downstream compiler for generated code that will have the prelude applied
     * to it.
     * &#64;param outPrelude  On exit holds a blob that holds the string of the prelude.
     *
     * Vtable slot {&#64;value #VT_getDownstreamCompilerPrelude}.
     */
    public static void getDownstreamCompilerPrelude(MemorySegment self, int passThrough, MemorySegment outPrelude) {
        try {
            MH_V_AIA.invokeExact(FfiSupport.fnPtr(self, VT_getDownstreamCompilerPrelude), self, passThrough, outPrelude);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the build version 'tag' string. The string is the same as produced via `git describe
     * --tags` for the project. If Slang is built separately from the automated build scripts the
     * contents will by default be 'unknown'. Any string can be set by changing the contents of
     * 'slang-tag-version.h' file and recompiling the project.
     * 
     * This method will return exactly the same result as the free function spGetBuildTagString.
     * 
     * &#64;return The build tag string
     *
     * Vtable slot {&#64;value #VT_getBuildTagString}.
     */
    public static MemorySegment getBuildTagString(MemorySegment self) {
        try {
            return (MemorySegment) MH_A_A.invokeExact(FfiSupport.fnPtr(self, VT_getBuildTagString), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Vtable slot {&#64;value #VT_setDefaultDownstreamCompiler}.
     */
    public static int setDefaultDownstreamCompiler(MemorySegment self, int sourceLanguage, int defaultCompiler) {
        try {
            return (int) MH_I_AII.invokeExact(FfiSupport.fnPtr(self, VT_setDefaultDownstreamCompiler), self, sourceLanguage, defaultCompiler);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Vtable slot {&#64;value #VT_getDefaultDownstreamCompiler}.
     */
    public static int getDefaultDownstreamCompiler(MemorySegment self, int sourceLanguage) {
        try {
            return (int) MH_I_AI.invokeExact(FfiSupport.fnPtr(self, VT_getDefaultDownstreamCompiler), self, sourceLanguage);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Vtable slot {&#64;value #VT_setLanguagePrelude}.
     */
    public static void setLanguagePrelude(MemorySegment self, int sourceLanguage, MemorySegment preludeText) {
        try {
            MH_V_AIA.invokeExact(FfiSupport.fnPtr(self, VT_setLanguagePrelude), self, sourceLanguage, preludeText);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the 'prelude' associated with a specific source language.
     * &#64;param sourceLanguage The language the prelude should be inserted on.
     * &#64;param outPrelude  On exit holds a blob that holds the string of the prelude.
     *
     * Vtable slot {&#64;value #VT_getLanguagePrelude}.
     */
    public static void getLanguagePrelude(MemorySegment self, int sourceLanguage, MemorySegment outPrelude) {
        try {
            MH_V_AIA.invokeExact(FfiSupport.fnPtr(self, VT_getLanguagePrelude), self, sourceLanguage, outPrelude);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Create a compile request.
     *
     * Vtable slot {&#64;value #VT_createCompileRequest}.
     */
    public static int createCompileRequest(MemorySegment self, MemorySegment outCompileRequest) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_createCompileRequest), self, outCompileRequest);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Add new builtin declarations to be used in subsequent compiles.
     *
     * Vtable slot {&#64;value #VT_addBuiltins}.
     */
    public static void addBuiltins(MemorySegment self, MemorySegment sourcePath, MemorySegment sourceString) {
        try {
            MH_V_AAA.invokeExact(FfiSupport.fnPtr(self, VT_addBuiltins), self, sourcePath, sourceString);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Set the session shared library loader. If this changes the loader, it may cause shared
     * libraries to be unloaded
     * &#64;param loader The loader to set. Setting nullptr sets the default loader.
     *
     * Vtable slot {&#64;value #VT_setSharedLibraryLoader}.
     */
    public static void setSharedLibraryLoader(MemorySegment self, MemorySegment loader) {
        try {
            MH_V_AA.invokeExact(FfiSupport.fnPtr(self, VT_setSharedLibraryLoader), self, loader);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Gets the currently set shared library loader
     * &#64;return Gets the currently set loader. If returns nullptr, it's the default loader
     *
     * Vtable slot {&#64;value #VT_getSharedLibraryLoader}.
     */
    public static MemorySegment getSharedLibraryLoader(MemorySegment self) {
        try {
            return (MemorySegment) MH_A_A.invokeExact(FfiSupport.fnPtr(self, VT_getSharedLibraryLoader), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Returns SLANG_OK if the compilation target is supported for this session
     * 
     * &#64;param target The compilation target to test
     * &#64;return SLANG_OK if the target is available
     * SLANG_E_NOT_IMPLEMENTED if not implemented in this build
     * SLANG_E_NOT_FOUND if other resources (such as shared libraries) required to make target work
     * could not be found SLANG_FAIL other kinds of failures
     *
     * Vtable slot {&#64;value #VT_checkCompileTargetSupport}.
     */
    public static int checkCompileTargetSupport(MemorySegment self, int target) {
        try {
            return (int) MH_I_AI.invokeExact(FfiSupport.fnPtr(self, VT_checkCompileTargetSupport), self, target);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Returns SLANG_OK if the pass through support is supported for this session
     * &#64;param session Session
     * &#64;param target The compilation target to test
     * &#64;return SLANG_OK if the target is available
     * SLANG_E_NOT_IMPLEMENTED if not implemented in this build
     * SLANG_E_NOT_FOUND if other resources (such as shared libraries) required to make target work
     * could not be found SLANG_FAIL other kinds of failures
     *
     * Vtable slot {&#64;value #VT_checkPassThroughSupport}.
     */
    public static int checkPassThroughSupport(MemorySegment self, int passThrough) {
        try {
            return (int) MH_I_AI.invokeExact(FfiSupport.fnPtr(self, VT_checkPassThroughSupport), self, passThrough);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Compile from (embedded source) the core module on the session.
     * Will return a failure if there is already a core module available
     * NOTE! API is experimental and not ready for production code
     * &#64;param flags to control compilation
     *
     * Vtable slot {&#64;value #VT_compileCoreModule}.
     */
    public static int compileCoreModule(MemorySegment self, int flags) {
        try {
            return (int) MH_I_AI.invokeExact(FfiSupport.fnPtr(self, VT_compileCoreModule), self, flags);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Load the core module. Currently loads modules from the file system.
     * &#64;param coreModule Start address of the serialized core module
     * &#64;param coreModuleSizeInBytes The size in bytes of the serialized core module
     * 
     * NOTE! API is experimental and not ready for production code
     *
     * Vtable slot {&#64;value #VT_loadCoreModule}.
     */
    public static int loadCoreModule(MemorySegment self, MemorySegment coreModule, long coreModuleSizeInBytes) {
        try {
            return (int) MH_I_AAz.invokeExact(FfiSupport.fnPtr(self, VT_loadCoreModule), self, coreModule, coreModuleSizeInBytes);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Save the core module to the file system
     * &#64;param archiveType The type of archive used to hold the core module
     * &#64;param outBlob The serialized blob containing the core module
     * 
     * NOTE! API is experimental and not ready for production code
     *
     * Vtable slot {&#64;value #VT_saveCoreModule}.
     */
    public static int saveCoreModule(MemorySegment self, int archiveType, MemorySegment outBlob) {
        try {
            return (int) MH_I_AIA.invokeExact(FfiSupport.fnPtr(self, VT_saveCoreModule), self, archiveType, outBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Look up the internal ID of a capability by its `name`.
     * 
     * Capability IDs are *not* guaranteed to be stable across versions
     * of the Slang library, so clients are expected to look up
     * capabilities by name at runtime.
     *
     * Vtable slot {&#64;value #VT_findCapability}.
     */
    public static int findCapability(MemorySegment self, MemorySegment name) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_findCapability), self, name);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Set the downstream/pass through compiler to be used for a transition from the source type to
     * the target type
     * &#64;param source The source 'code gen target'
     * &#64;param target The target 'code gen target'
     * &#64;param compiler The compiler/pass through to use for the transition from source to target
     *
     * Vtable slot {&#64;value #VT_setDownstreamCompilerForTransition}.
     */
    public static void setDownstreamCompilerForTransition(MemorySegment self, int source, int target, int compiler) {
        try {
            MH_V_AIII.invokeExact(FfiSupport.fnPtr(self, VT_setDownstreamCompilerForTransition), self, source, target, compiler);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the downstream/pass through compiler for a transition specified by source and target
     * &#64;param source The source 'code gen target'
     * &#64;param target The target 'code gen target'
     * &#64;return The compiler that is used for the transition. Returns SLANG_PASS_THROUGH_NONE it is not
     * defined
     *
     * Vtable slot {&#64;value #VT_getDownstreamCompilerForTransition}.
     */
    public static int getDownstreamCompilerForTransition(MemorySegment self, int source, int target) {
        try {
            return (int) MH_I_AII.invokeExact(FfiSupport.fnPtr(self, VT_getDownstreamCompilerForTransition), self, source, target);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the time in seconds spent in the slang and downstream compiler.
     *
     * Vtable slot {&#64;value #VT_getCompilerElapsedTime}.
     */
    public static void getCompilerElapsedTime(MemorySegment self, MemorySegment outTotalTime, MemorySegment outDownstreamTime) {
        try {
            MH_V_AAA.invokeExact(FfiSupport.fnPtr(self, VT_getCompilerElapsedTime), self, outTotalTime, outDownstreamTime);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Specify a spirv.core.grammar.json file to load and use when
     * parsing and checking any SPIR-V code
     *
     * Vtable slot {&#64;value #VT_setSPIRVCoreGrammar}.
     */
    public static int setSPIRVCoreGrammar(MemorySegment self, MemorySegment jsonPath) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_setSPIRVCoreGrammar), self, jsonPath);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Parse slangc command line options into a SessionDesc that can be used to create a session
     * with all the compiler options specified in the command line.
     * &#64;param argc The number of command line arguments.
     * &#64;param argv An input array of command line arguments to parse.
     * &#64;param outSessionDesc A pointer to a SessionDesc struct to receive parsed session desc.
     * &#64;param outAuxAllocation Auxiliary memory allocated to hold data used in the session desc.
     *
     * Vtable slot {&#64;value #VT_parseCommandLineArguments}.
     */
    public static int parseCommandLineArguments(MemorySegment self, int argc, MemorySegment argv, MemorySegment outSessionDesc, MemorySegment outAuxAllocation) {
        try {
            return (int) MH_I_AIAAA.invokeExact(FfiSupport.fnPtr(self, VT_parseCommandLineArguments), self, argc, argv, outSessionDesc, outAuxAllocation);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Computes a digest that uniquely identifies the session description.
     *
     * Vtable slot {&#64;value #VT_getSessionDescDigest}.
     */
    public static int getSessionDescDigest(MemorySegment self, MemorySegment sessionDesc, MemorySegment outBlob) {
        try {
            return (int) MH_I_AAA.invokeExact(FfiSupport.fnPtr(self, VT_getSessionDescDigest), self, sessionDesc, outBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Compile from (embedded source) the builtin module on the session.
     * Will return a failure if there is already a builtin module available.
     * NOTE! API is experimental and not ready for production code.
     * &#64;param module The builtin module name.
     * &#64;param flags to control compilation
     *
     * Vtable slot {&#64;value #VT_compileBuiltinModule}.
     */
    public static int compileBuiltinModule(MemorySegment self, int module, int flags) {
        try {
            return (int) MH_I_AII.invokeExact(FfiSupport.fnPtr(self, VT_compileBuiltinModule), self, module, flags);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Load a builtin module. Currently loads modules from the file system.
     * &#64;param module The builtin module name
     * &#64;param moduleData Start address of the serialized core module
     * &#64;param sizeInBytes The size in bytes of the serialized builtin module
     * 
     * NOTE! API is experimental and not ready for production code
     *
     * Vtable slot {&#64;value #VT_loadBuiltinModule}.
     */
    public static int loadBuiltinModule(MemorySegment self, int module, MemorySegment moduleData, long sizeInBytes) {
        try {
            return (int) MH_I_AIAz.invokeExact(FfiSupport.fnPtr(self, VT_loadBuiltinModule), self, module, moduleData, sizeInBytes);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Save the builtin module to the file system
     * &#64;param module The builtin module name
     * &#64;param archiveType The type of archive used to hold the builtin module
     * &#64;param outBlob The serialized blob containing the builtin module
     * 
     * NOTE! API is experimental and not ready for production code
     *
     * Vtable slot {&#64;value #VT_saveBuiltinModule}.
     */
    public static int saveBuiltinModule(MemorySegment self, int module, int archiveType, MemorySegment outBlob) {
        try {
            return (int) MH_I_AIIA.invokeExact(FfiSupport.fnPtr(self, VT_saveBuiltinModule), self, module, archiveType, outBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Get the version of the downstream/pass-through compiler that Slang will actually load and
     * use for `passThrough`, applying the same lazy discovery and library search order used during
     * compilation. This lets a client key its behavior off the exact library Slang selected (for
     * example, the specific NVRTC that will compile CUDA), which can differ from a version the client
     * might discover on its own.
     * 
     * This is not a cheap accessor: the first call for a given `passThrough` performs discovery and
     * loads the downstream library into the process (then memoizes it for subsequent calls).
     * 
     * Only some downstream compilers report a numeric version (e.g. NVRTC, DXC, the C/C++ toolchains);
     * others (e.g. the glslang family and Tint) always report `(0,0)`. The version is read uniformly
     * from the loaded compiler's descriptor, so a versionless-but-loaded compiler still returns
     * SLANG_OK with major/minor 0 - which the result alone does not distinguish from a genuine 0.0.
     * &#64;param passThrough The downstream compiler to query (e.g. SLANG_PASS_THROUGH_NVRTC).
     * &#64;param outMajor Receives the major version number. May be null.
     * &#64;param outMinor Receives the minor version number. May be null.
     * &#64;return SLANG_OK if the compiler was located and loaded (see the versionless note above).
     * SLANG_E_NOT_FOUND if the compiler could not be located or loaded, and likewise for
     * SLANG_PASS_THROUGH_NONE or an out-of-range value - the result code alone does not distinguish an
     * invalid argument from a compiler that is simply not installed.
     *
     * Vtable slot {&#64;value #VT_getDownstreamCompilerVersion}.
     */
    public static int getDownstreamCompilerVersion(MemorySegment self, int passThrough, MemorySegment outMajor, MemorySegment outMinor) {
        try {
            return (int) MH_I_AIAA.invokeExact(FfiSupport.fnPtr(self, VT_getDownstreamCompilerVersion), self, passThrough, outMajor, outMinor);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

}
