// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Argument used for specialization to types/values.
 *
 * Layout of C struct {&#64;code SpecializationArg} (size 16, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class SpecializationArg {
    private SpecializationArg() {}

    public static final long SIZE = 16;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_kind = 0;
    public static final long OFFSET_type = 8;
    public static final long OFFSET_expr = 8;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    /**
     * The kind of specialization argument.
     */
    public static int getKind(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_kind);
    }

    public static void setKind(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_kind, value);
    }

    /**
     * A type specialization argument, used for `Kind::Type`.
     */
    public static MemorySegment getType(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_type);
    }

    public static void setType(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_type, value);
    }

    /**
     * An expression in Slang syntax, used for `Kind::Expr`.
     */
    public static MemorySegment getExpr(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_expr);
    }

    public static void setExpr(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_expr, value);
    }

}
