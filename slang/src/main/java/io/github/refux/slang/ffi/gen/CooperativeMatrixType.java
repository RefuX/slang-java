// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Layout of C struct {&#64;code CooperativeMatrixType} (size 20, alignment 4; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class CooperativeMatrixType {
    private CooperativeMatrixType() {}

    public static final long SIZE = 20;
    public static final long ALIGNMENT = 4;

    public static final long OFFSET_componentType = 0;
    public static final long OFFSET_scope = 4;
    public static final long OFFSET_rowCount = 8;
    public static final long OFFSET_columnCount = 12;
    public static final long OFFSET_use = 16;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static int getComponentType(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_componentType);
    }

    public static void setComponentType(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_componentType, value);
    }

    public static int getScope(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_scope);
    }

    public static void setScope(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_scope, value);
    }

    public static int getRowCount(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_rowCount);
    }

    public static void setRowCount(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_rowCount, value);
    }

    public static int getColumnCount(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_columnCount);
    }

    public static void setColumnCount(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_columnCount, value);
    }

    public static int getUse(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_use);
    }

    public static void setUse(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_use, value);
    }

}
