// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodType;
import static java.lang.foreign.ValueLayout.*;

/**
 * Raw vtable dispatch for COM interface {&#64;code ISlangClonable} (base {&#64;code ISlangCastable}, vtable size 5). Methods are static and take the COM object pointer as {&#64;code self}; inherited slots live on the base interface's class. The hand-written wrappers in {&#64;code ffi} add ownership and ergonomics.
 */
public final class ISlangClonable {
    private ISlangClonable() {}

    /** SLANG_COM_INTERFACE uuid, for queryInterface. */
    public static final String IID = "1ec36168-e9f4-430d-bb17-048a8046b31f";

    public static final int VT_clone = 4;

    private static final MethodHandle MH_A_AA =
        FfiSupport.unbound(FunctionDescriptor.of(ADDRESS, ADDRESS, ADDRESS));

    /**
     * Note the use of guid is for the desired interface/object.
     * The object is returned *not* ref counted. Any type that can implements the interface,
     * derives from ICastable, and so (not withstanding some other issue) will always return
     * an ICastable interface which other interfaces/types are accessible from via castAs
     *
     * Vtable slot {&#64;value #VT_clone}.
     */
    public static MemorySegment clone(MemorySegment self, MemorySegment guid) {
        try {
            return (MemorySegment) MH_A_AA.invokeExact(FfiSupport.fnPtr(self, VT_clone), self, guid);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

}
