// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodType;
import static java.lang.foreign.ValueLayout.*;

/**
 * Raw vtable dispatch for COM interface {&#64;code ISyntheticResourceMetadata} (base {&#64;code ISlangCastable}, vtable size 7). Methods are static and take the COM object pointer as {&#64;code self}; inherited slots live on the base interface's class. The hand-written wrappers in {&#64;code ffi} add ownership and ergonomics.
 */
public final class ISyntheticResourceMetadata {
    private ISyntheticResourceMetadata() {}

    /** SLANG_COM_INTERFACE uuid, for queryInterface. */
    public static final String IID = "47a33723-181b-4d2b-b89e-215495bb388b";

    public static final int VT_getResourceCount = 4;
    public static final int VT_getResourceInfo = 5;
    public static final int VT_findResourceIndexByID = 6;

    private static final MethodHandle MH_I_A =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_I_AIA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, ADDRESS));

    /**
     * Number of synthetic bindable resources reported by this
     * metadata object.
     *
     * Vtable slot {&#64;value #VT_getResourceCount}.
     */
    public static int getResourceCount(MemorySegment self) {
        try {
            return (int) MH_I_A.invokeExact(FfiSupport.fnPtr(self, VT_getResourceCount), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Populate `outInfo` with the metadata for synthetic resource
     * `index`. The caller should default-initialize `outInfo` (for
     * example `SyntheticResourceInfo info;`) so `structSize` is set
     * to `sizeof(SyntheticResourceInfo)`.
     * Returns `SLANG_OK` on success,
     * `SLANG_E_INVALID_ARG` for null `outInfo`, mismatched
     * `structSize`, or out-of-range `index`.
     *
     * Vtable slot {&#64;value #VT_getResourceInfo}.
     */
    public static int getResourceInfo(MemorySegment self, int index, MemorySegment outInfo) {
        try {
            return (int) MH_I_AIA.invokeExact(FfiSupport.fnPtr(self, VT_getResourceInfo), self, index, outInfo);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Look up the resource index for a stable synthetic resource
     * identifier. Returns `SLANG_OK` on success,
     * `SLANG_E_NOT_FOUND` when no resource with that id exists, and
     * `SLANG_E_INVALID_ARG` for null `outIndex`.
     *
     * Vtable slot {&#64;value #VT_findResourceIndexByID}.
     */
    public static int findResourceIndexByID(MemorySegment self, int id, MemorySegment outIndex) {
        try {
            return (int) MH_I_AIA.invokeExact(FfiSupport.fnPtr(self, VT_findResourceIndexByID), self, id, outIndex);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

}
