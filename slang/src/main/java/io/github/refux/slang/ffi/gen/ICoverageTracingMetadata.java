// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodType;
import static java.lang.foreign.ValueLayout.*;

/**
 * Raw vtable dispatch for COM interface {&#64;code ICoverageTracingMetadata} (base {&#64;code ISlangCastable}, vtable size 8). Methods are static and take the COM object pointer as {&#64;code self}; inherited slots live on the base interface's class. The hand-written wrappers in {&#64;code ffi} add ownership and ergonomics.
 */
public final class ICoverageTracingMetadata {
    private ICoverageTracingMetadata() {}

    /** SLANG_COM_INTERFACE uuid, for queryInterface. */
    public static final String IID = "7c9f1d50-1e4a-4b9c-8e21-3f7b82a3d951";

    public static final int VT_getCounterCount = 4;
    public static final int VT_getEntryInfo = 5;
    public static final int VT_getBufferInfo = 6;
    public static final int VT_getEntryCount = 7;

    private static final MethodHandle MH_I_A =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_I_AA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_I_AIA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT, ADDRESS));

    /**
     * Number of runtime counter slots in the synthesized coverage
     * buffer. This can differ from `getEntryCount()` once a coverage
     * mode has counterless metadata entries, shares one counter across
     * several source entries, or reports entries whose counts are
     * derived from other counters.
     *
     * Vtable slot {&#64;value #VT_getCounterCount}.
     */
    public static int getCounterCount(MemorySegment self) {
        try {
            return (int) MH_I_A.invokeExact(FfiSupport.fnPtr(self, VT_getCounterCount), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Populate `outInfo` with attribution info for source coverage
     * entry `index`. The valid range is `[0, getEntryCount())`. The
     * caller must pre-set `outInfo-&gt;structSize` to the size of the
     * `CoverageEntryInfo` definition it was compiled against. The
     * implementation accepts any prefix-compatible size at or above
     * the v1 minimum (`file` and `line`), and only writes tail fields
     * that fit in the caller-provided struct. Returns `SLANG_OK` on
     * success, `SLANG_E_INVALID_ARG` for null `outInfo`, too-small
     * `structSize`, or out-of-range `index`.
     *
     * Vtable slot {&#64;value #VT_getEntryInfo}.
     */
    public static int getEntryInfo(MemorySegment self, int index, MemorySegment outInfo) {
        try {
            return (int) MH_I_AIA.invokeExact(FfiSupport.fnPtr(self, VT_getEntryInfo), self, index, outInfo);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Populate `outInfo` with the coverage buffer's descriptor
     * binding info. The caller should default-initialize `outInfo`
     * (for example `CoverageBufferInfo info;`) so `structSize` is
     * set to `sizeof(CoverageBufferInfo)`.
     * Returns `SLANG_OK` on success, `SLANG_E_INVALID_ARG` for null
     * `outInfo` or mismatched `structSize`.
     * 
     * This is the coverage-specific compatibility view of the hidden
     * buffer. Hosts that want the generic hidden-resource contract
     * should query `ISyntheticResourceMetadata` from the same
     * `IMetadata` object.
     *
     * Vtable slot {&#64;value #VT_getBufferInfo}.
     */
    public static int getBufferInfo(MemorySegment self, MemorySegment outInfo) {
        try {
            return (int) MH_I_AA.invokeExact(FfiSupport.fnPtr(self, VT_getBufferInfo), self, outInfo);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

    /**
     * Number of source coverage entries available through
     * `getEntryInfo`. The current line/function/branch producers have
     * one entry per counter, but future source-region coverage may
     * expose entries that do not map one-to-one with runtime counter
     * slots.
     *
     * Vtable slot {&#64;value #VT_getEntryCount}.
     */
    public static int getEntryCount(MemorySegment self) {
        try {
            return (int) MH_I_A.invokeExact(FfiSupport.fnPtr(self, VT_getEntryCount), self);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

}
