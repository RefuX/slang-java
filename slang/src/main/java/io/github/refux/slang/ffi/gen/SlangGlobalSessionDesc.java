// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Layout of C struct {&#64;code SlangGlobalSessionDesc} (size 80, alignment 4; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class SlangGlobalSessionDesc {
    private SlangGlobalSessionDesc() {}

    public static final long SIZE = 80;
    public static final long ALIGNMENT = 4;

    public static final long OFFSET_structureSize = 0;
    public static final long OFFSET_apiVersion = 4;
    public static final long OFFSET_minLanguageVersion = 8;
    public static final long OFFSET_enableGLSL = 12;
    public static final long OFFSET_reserved = 16;

    /** Allocates one zero-initialized instance (structureSize prefilled, per the versioned-struct convention). */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        setStructureSize(self, (int) SIZE); // versioned-struct convention
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        for (int i = 0; i < count; i++) {
            setStructureSize(element(array, i), (int) SIZE); // versioned-struct convention
        }
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    /**
     * Size of this struct.
     */
    public static int getStructureSize(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_structureSize);
    }

    public static void setStructureSize(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_structureSize, value);
    }

    /**
     * Slang API version.
     */
    public static int getApiVersion(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_apiVersion);
    }

    public static void setApiVersion(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_apiVersion, value);
    }

    /**
     * Specify the oldest Slang language version that any sessions will use.
     */
    public static int getMinLanguageVersion(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_minLanguageVersion);
    }

    public static void setMinLanguageVersion(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_minLanguageVersion, value);
    }

    /**
     * Whether to enable GLSL support.
     */
    public static boolean getEnableGLSL(MemorySegment self) {
        return self.get(JAVA_BOOLEAN, OFFSET_enableGLSL);
    }

    public static void setEnableGLSL(MemorySegment self, boolean value) {
        self.set(JAVA_BOOLEAN, OFFSET_enableGLSL, value);
    }

    /**
     * Reserved for future use.
     */
    public static MemorySegment reserved(MemorySegment self) {
        return self.asSlice(OFFSET_reserved, 64);
    }

}
