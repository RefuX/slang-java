// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * A "Universally Unique Identifier" (UUID)
 * 
 * The Slang API uses UUIDs to identify interfaces when
 * using `queryInterface`.
 * 
 * This type is compatible with the `GUID` type defined
 * by the Component Object Model (COM), but Slang is
 * not dependent on COM.
 *
 * Layout of C struct {&#64;code SlangUUID} (size 16, alignment 4; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class SlangUUID {
    private SlangUUID() {}

    public static final long SIZE = 16;
    public static final long ALIGNMENT = 4;

    public static final long OFFSET_data1 = 0;
    public static final long OFFSET_data2 = 4;
    public static final long OFFSET_data3 = 6;
    public static final long OFFSET_data4 = 8;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static int getData1(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_data1);
    }

    public static void setData1(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_data1, value);
    }

    public static short getData2(MemorySegment self) {
        return self.get(JAVA_SHORT, OFFSET_data2);
    }

    public static void setData2(MemorySegment self, short value) {
        self.set(JAVA_SHORT, OFFSET_data2, value);
    }

    public static short getData3(MemorySegment self) {
        return self.get(JAVA_SHORT, OFFSET_data3);
    }

    public static void setData3(MemorySegment self, short value) {
        self.set(JAVA_SHORT, OFFSET_data3, value);
    }

    public static MemorySegment data4(MemorySegment self) {
        return self.asSlice(OFFSET_data4, 8);
    }

}
