// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Layout of C struct {&#64;code SyntheticResourceInfo} (size 56, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class SyntheticResourceInfo {
    private SyntheticResourceInfo() {}

    public static final long SIZE = 56;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_structSize = 0;
    public static final long OFFSET_id = 8;
    public static final long OFFSET_bindingType = 12;
    public static final long OFFSET_arraySize = 16;
    public static final long OFFSET_scope = 20;
    public static final long OFFSET_access = 24;
    public static final long OFFSET_entryPointIndex = 28;
    public static final long OFFSET_space = 32;
    public static final long OFFSET_binding = 36;
    public static final long OFFSET_uniformOffset = 40;
    public static final long OFFSET_uniformStride = 44;
    public static final long OFFSET_debugName = 48;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static long getStructSize(MemorySegment self) {
        return self.get(FfiSupport.C_SIZE_T, OFFSET_structSize);
    }

    public static void setStructSize(MemorySegment self, long value) {
        self.set(FfiSupport.C_SIZE_T, OFFSET_structSize, value);
    }

    /**
     * Stable, opaque, non-zero synthetic resource identifier within
     * the compiled program. `0` is reserved as the default
     * "unassigned" sentinel; `findResourceIndexByID(0, ...)` always
     * returns `SLANG_E_NOT_FOUND`. Non-zero ids are assigned from the
     * compiler's internal synthetic-resource registry; hosts should
     * treat the numeric value as opaque and use it only to correlate
     * metadata and runtime binding helpers.
     */
    public static int getId(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_id);
    }

    public static void setId(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_id, value);
    }

    /**
     * The Slang binding kind represented by this synthetic
     * resource.
     */
    public static int getBindingType(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_bindingType);
    }

    public static void setBindingType(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_bindingType, value);
    }

    /**
     * Number of logical resources in the synthetic binding. Most
     * current instrumentation resources are scalar (`1`).
     */
    public static int getArraySize(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_arraySize);
    }

    public static void setArraySize(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_arraySize, value);
    }

    /**
     * Whether the resource is global/root-scoped or attached to a
     * specific entry point. Coverage currently reports a global
     * resource; entry-point scoped resources are reserved for future
     * synthetic-resource producers.
     */
    public static int getScope(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_scope);
    }

    public static void setScope(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_scope, value);
    }

    /**
     * Intended access pattern for the resource.
     */
    public static int getAccess(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_access);
    }

    public static void setAccess(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_access, value);
    }

    /**
     * Entry point index when `scope == EntryPoint`, else `-1`.
     * No current coverage resource uses entry-point scope.
     */
    public static int getEntryPointIndex(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_entryPointIndex);
    }

    public static void setEntryPointIndex(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_entryPointIndex, value);
    }

    /**
     * Sentinel conventions:
     * - `space == -1` means the target does not report a descriptor
     * space for this resource
     * - `binding == -1` means descriptor binding is unavailable for
     * this target
     * - `uniformOffset == -1` means CPU/CUDA-style marshaling
     * location is unavailable for this target
     * - `uniformStride == 0` means CPU/CUDA-style marshaling
     * location is unavailable or no stride applies; when
     * `uniformOffset &gt;= 0`, a non-zero stride may be reported
     * even for scalar resources
     * `0` is a valid value for `space`, `binding`, and
     * `uniformOffset`.
     * 
     * Descriptor-facing location for backends that bind synthetic
     * resources via `(space, binding)`.
     */
    public static int getSpace(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_space);
    }

    public static void setSpace(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_space, value);
    }

    public static int getBinding(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_binding);
    }

    public static void setBinding(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_binding, value);
    }

    /**
     * CPU/CUDA-style marshaling location in generated uniform /
     * wrapper parameter data, in bytes.
     */
    public static int getUniformOffset(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_uniformOffset);
    }

    public static void setUniformOffset(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_uniformOffset, value);
    }

    /**
     * Byte stride between adjacent logical elements when
     * CPU/CUDA-style marshaling is reported.
     */
    public static int getUniformStride(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_uniformStride);
    }

    public static void setUniformStride(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_uniformStride, value);
    }

    /**
     * Optional stable debug name for the synthetic resource. The
     * returned pointer is valid for the lifetime of the metadata
     * object.
     */
    public static MemorySegment getDebugName(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_debugName);
    }

    public static void setDebugName(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_debugName, value);
    }

}
