// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Description of a code generation target.
 *
 * Layout of C struct {&#64;code TargetDesc} (size 48, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class TargetDesc {
    private TargetDesc() {}

    public static final long SIZE = 48;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_structureSize = 0;
    public static final long OFFSET_format = 8;
    public static final long OFFSET_profile = 12;
    public static final long OFFSET_flags = 16;
    public static final long OFFSET_floatingPointMode = 20;
    public static final long OFFSET_lineDirectiveMode = 24;
    public static final long OFFSET_forceGLSLScalarBufferLayout = 28;
    public static final long OFFSET_compilerOptionEntries = 32;
    public static final long OFFSET_compilerOptionEntryCount = 40;

    /** Allocates one zero-initialized instance (structureSize prefilled, per the versioned-struct convention). */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        setStructureSize(self, (long) SIZE); // versioned-struct convention
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        for (int i = 0; i < count; i++) {
            setStructureSize(element(array, i), (long) SIZE); // versioned-struct convention
        }
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    /**
     * The size of this structure, in bytes.
     */
    public static long getStructureSize(MemorySegment self) {
        return self.get(FfiSupport.C_SIZE_T, OFFSET_structureSize);
    }

    public static void setStructureSize(MemorySegment self, long value) {
        self.set(FfiSupport.C_SIZE_T, OFFSET_structureSize, value);
    }

    /**
     * The target format to generate code for (e.g., SPIR-V, DXIL, etc.)
     */
    public static int getFormat(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_format);
    }

    public static void setFormat(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_format, value);
    }

    /**
     * The compilation profile supported by the target (e.g., "Shader Model 5.1")
     */
    public static int getProfile(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_profile);
    }

    public static void setProfile(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_profile, value);
    }

    /**
     * Flags for the code generation target. Currently unused.
     */
    public static int getFlags(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_flags);
    }

    public static void setFlags(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_flags, value);
    }

    /**
     * Default mode to use for floating-point operations on the target.
     */
    public static int getFloatingPointMode(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_floatingPointMode);
    }

    public static void setFloatingPointMode(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_floatingPointMode, value);
    }

    /**
     * The line directive mode for output source code.
     */
    public static int getLineDirectiveMode(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_lineDirectiveMode);
    }

    public static void setLineDirectiveMode(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_lineDirectiveMode, value);
    }

    /**
     * Whether to force `scalar` layout for glsl shader storage buffers.
     */
    public static boolean getForceGLSLScalarBufferLayout(MemorySegment self) {
        return self.get(JAVA_BOOLEAN, OFFSET_forceGLSLScalarBufferLayout);
    }

    public static void setForceGLSLScalarBufferLayout(MemorySegment self, boolean value) {
        self.set(JAVA_BOOLEAN, OFFSET_forceGLSLScalarBufferLayout, value);
    }

    /**
     * Pointer to an array of compiler option entries, whose size is compilerOptionEntryCount.
     */
    public static MemorySegment getCompilerOptionEntries(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_compilerOptionEntries);
    }

    public static void setCompilerOptionEntries(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_compilerOptionEntries, value);
    }

    /**
     * Number of additional compiler option entries.
     */
    public static int getCompilerOptionEntryCount(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_compilerOptionEntryCount);
    }

    public static void setCompilerOptionEntryCount(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_compilerOptionEntryCount, value);
    }

}
