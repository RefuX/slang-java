// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Layout of C struct {&#64;code VMExecInstHeader} (size 16, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class VMExecInstHeader {
    private VMExecInstHeader() {}

    public static final long SIZE = 16;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_functionPtr = 0;
    public static final long OFFSET_opcodeExtension = 8;
    public static final long OFFSET_operandCount = 12;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static MemorySegment getFunctionPtr(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_functionPtr);
    }

    public static void setFunctionPtr(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_functionPtr, value);
    }

    public static int getOpcodeExtension(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_opcodeExtension);
    }

    public static void setOpcodeExtension(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_opcodeExtension, value);
    }

    public static int getOperandCount(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_operandCount);
    }

    public static void setOperandCount(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_operandCount, value);
    }

}
