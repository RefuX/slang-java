// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodType;
import static java.lang.foreign.ValueLayout.*;

/**
 * A (real or virtual) file system.
 * 
 * Slang can make use of this interface whenever it would otherwise try to load files
 * from disk, allowing applications to hook and/or override filesystem access from
 * the compiler.
 * 
 * It is the responsibility of
 * the caller of any method that returns a ISlangBlob to release the blob when it is no
 * longer used (using 'release').
 *
 * Raw vtable dispatch for COM interface {&#64;code ISlangFileSystem} (base {&#64;code ISlangCastable}, vtable size 5). Methods are static and take the COM object pointer as {&#64;code self}; inherited slots live on the base interface's class. The hand-written wrappers in {&#64;code ffi} add ownership and ergonomics.
 */
public final class ISlangFileSystem {
    private ISlangFileSystem() {}

    /** SLANG_COM_INTERFACE uuid, for queryInterface. */
    public static final String IID = "003a09fc-3a4d-4ba0-ad60-1fd863a915ab";

    public static final int VT_loadFile = 4;

    private static final MethodHandle MH_I_AAA =
        FfiSupport.unbound(FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));

    /**
     * Load a file from `path` and return a blob of its contents
     * &#64;param path The path to load from, as a null-terminated UTF-8 string.
     * &#64;param outBlob A destination pointer to receive the blob of the file contents.
     * &#64;returns A `SlangResult` to indicate success or failure in loading the file.
     * 
     * NOTE! This is a *binary* load - the blob should contain the exact same bytes
     * as are found in the backing file.
     * 
     * If load is successful, the implementation should create a blob to hold
     * the file's content, store it to `outBlob`, and return 0.
     * If the load fails, the implementation should return a failure status
     * (any negative value will do).
     *
     * Vtable slot {&#64;value #VT_loadFile}.
     */
    public static int loadFile(MemorySegment self, MemorySegment path, MemorySegment outBlob) {
        try {
            return (int) MH_I_AAA.invokeExact(FfiSupport.fnPtr(self, VT_loadFile), self, path, outBlob);
        } catch (Throwable t) {
            throw FfiSupport.rethrow(t);
        }
    }

}
