// GENERATED by slang-bindgen from api/slang-api.json (Slang 2026.13). DO NOT EDIT.
// Regenerate: see CLAUDE.md / DESIGN.md §9.
package io.github.refux.slang.ffi.gen;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import static java.lang.foreign.ValueLayout.*;

/**
 * Layout of C struct {&#64;code VMExecOperand} (size 16, alignment 8; offsets clang-verified across all supported ABIs). Accessors are static and offset-based, so overlapping union members need no special casing.
 */
public final class VMExecOperand {
    private VMExecOperand() {}

    public static final long SIZE = 16;
    public static final long ALIGNMENT = 8;

    public static final long OFFSET_section = 0;
    public static final long OFFSET_type = 8;
    public static final long OFFSET_size = 9;
    public static final long OFFSET_offset = 12;

    /** Allocates one zero-initialized instance. */
    public static MemorySegment allocate(Arena arena) {
        MemorySegment self = arena.allocate(SIZE, ALIGNMENT);
        return self;
    }

    /** Allocates a contiguous zero-initialized array of {@code count} instances. */
    public static MemorySegment allocateArray(Arena arena, int count) {
        MemorySegment array = arena.allocate(SIZE * count, ALIGNMENT);
        return array;
    }

    /** The {@code index}-th element of an {@link #allocateArray} array. */
    public static MemorySegment element(MemorySegment array, int index) {
        return array.asSlice(SIZE * index, SIZE);
    }

    public static MemorySegment getSection(MemorySegment self) {
        return self.get(ADDRESS, OFFSET_section);
    }

    public static void setSection(MemorySegment self, MemorySegment value) {
        self.set(ADDRESS, OFFSET_section, value);
    }

    public static int getType(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_type);
    }

    public static void setType(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_type, value);
    }

    public static int getSize(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_size);
    }

    public static void setSize(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_size, value);
    }

    public static int getOffset(MemorySegment self) {
        return self.get(JAVA_INT, OFFSET_offset);
    }

    public static void setOffset(MemorySegment self, int value) {
        self.set(JAVA_INT, OFFSET_offset, value);
    }

}
